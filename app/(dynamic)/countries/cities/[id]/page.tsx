import PageContainer from "@/components/ui/pageContainer";
import React from "react";

const SingleCitypage = ({ params }) => {
  return <PageContainer>SingleCitypage id: {params.id}</PageContainer>;
};

export default SingleCitypage;
